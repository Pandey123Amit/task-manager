# Local PostgreSQL (Docker): run `docker compose up -d` then use this URL
DATABASE_URL="postgresql://taskmanager:taskmanager@localhost:5536/taskmanager?schema=public"

JWT_SECRET="change-me-to-a-long-random-string-in-production"
NEXT_PUBLIC_API_URL="http://localhost:3000"
# Same-origin (optional): NEXT_PUBLIC_API_URL=""
